<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#0F172A">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#1E293B"
        app:title="Support Chat"
        app:titleTextColor="#FFFFFF"
        app:navigationIcon="@drawable/ic_back_arrow"
        app:navigationIconTint="#FFFFFF"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/chatRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:background="#1E293B"
        android:gravity="center_vertical">

           <EditText
                android:id="@+id/etMessage"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/bg_border_blue_rounded"
                android:padding="12dp"
                android:hint="Type a message..."
                android:textColor="#FFFFFF"
                android:textColorHint="#94A3B8"
                android:textSize="14sp"
                android:maxLines="4"
                android:inputType="textCapSentences|textMultiLine"/>

            <ImageButton
                android:id="@+id/btnSend"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_clock" 
                android:tint="#3B82F6"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Send"/>
                <!-- Using ic_clock as placeholder for send icon if ic_send missing, 
                     but standard android usually has android.R.drawable.ic_menu_send but it's ugly.
                     I'll assume I can use a generic icon or create ic_send.xml quickly. -->
    </LinearLayout>
</LinearLayout>
